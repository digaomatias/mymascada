# =============================================================================
# MyMascada Demo - Environment Configuration
# =============================================================================
# Copy this file to .env and replace the placeholder secrets:
#   cp .env.demo .env
#
# Generate secrets (run each and paste into .env):
#   openssl rand -base64 32 | tr -d '\n'; echo   # for DB_PASSWORD
#   openssl rand -base64 64 | tr -d '\n'; echo   # for JWT_KEY
# =============================================================================

# --- Database ---
DB_USER=mymascada
DB_PASSWORD=CHANGE_ME_generate_with_openssl_rand
DB_NAME=mymascada_demo

# --- JWT ---
JWT_KEY=CHANGE_ME_generate_with_openssl_rand_base64_64
JWT_ISSUER=MyMascadaDemo
JWT_AUDIENCE=MyMascadaDemoUsers

# --- URLs ---
# For local testing, use localhost. For production demo, use your domain.
FRONTEND_URL=http://localhost:3001
PUBLIC_API_URL=http://localhost:5127
CORS_ALLOWED_ORIGINS=http://localhost:3001

# --- Demo Ports (avoid clashing with production) ---
DEMO_FRONTEND_PORT=3001
DEMO_API_PORT=5127

# --- Demo User ---
DEMO_EMAIL=demo@mymascada.com
DEMO_PASSWORD=DemoPass123!
DEMO_FIRST_NAME=Demo
DEMO_LAST_NAME=User

# --- Disabled Features ---
# These are hardcoded in docker-compose.demo.yml and do not need to be set here.
# Registration is blocked (invite code required, none configured).
# AI, OAuth, bank sync, and email are all disabled.
