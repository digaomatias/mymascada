# =============================================================================
# MyMascada - Self-Hosting Configuration
# =============================================================================
# Copy this file to .env and fill in the required values.
#
# Required values are marked with [REQUIRED] -- the app won't start without them.
# Everything else is optional and has sensible defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# Database [REQUIRED]
# -----------------------------------------------------------------------------
DB_USER=mymascada
DB_PASSWORD=CHANGE_ME
DB_NAME=mymascada

# -----------------------------------------------------------------------------
# JWT Authentication [REQUIRED]
# -----------------------------------------------------------------------------
# Secret key for signing JWT tokens. Must be at least 32 characters.
# Generate one with:  openssl rand -base64 64
JWT_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_64

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
# Public URL where users access the frontend (no trailing slash).
FRONTEND_URL=http://localhost:3000

# Host port for the frontend container (default: 3000).
FRONTEND_PORT=3000

# Host port for the API container (default: 5126).
# API_PORT=5126

# Public URL browsers use to reach the API.
# For localhost: http://localhost:5126
# With a reverse proxy: same as FRONTEND_URL (the proxy routes /api/* to the API).
PUBLIC_API_URL=http://localhost:5126

# CORS allowed origins (comma-separated). Must include FRONTEND_URL.
CORS_ALLOWED_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# AI Categorization -- OpenAI (Optional)
# -----------------------------------------------------------------------------
# Enables AI-powered transaction categorization.
# Get a key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-your-key-here
# OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Google OAuth (Optional)
# -----------------------------------------------------------------------------
# Enables "Sign in with Google" alongside email/password auth.
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Email Notifications (Optional)
# -----------------------------------------------------------------------------
# EMAIL_ENABLED=false
# EMAIL_PROVIDER=smtp
# EMAIL_FROM_ADDRESS=noreply@example.com
# EMAIL_FROM_NAME=MyMascada
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
